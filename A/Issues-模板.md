# Issue #1：[A] 项目文档与版本发布


**Assignee**：你自己

**描述**：

1. 编写 `README.md`：项目介绍、核心功能说明、技术栈、快速开始、项目结构、团队信息
2. 编写 `CONTRIBUTING.md`：分支策略、如何提交Issue、PR规范、Review流程
3. 确认 `LICENSE`（MIT）已存在
4. 所有成员PR合并后，打 Tag `v1.0.0`，创建 GitHub Release

**验收标准**：
- [ ] README.md 包含项目介绍、功能列表、快速开始步骤
- [ ] CONTRIBUTING.md 包含分支策略和PR流程说明
- [ ] Release 页面有 v1.0.0 Tag 和 Release Notes

---

# Issue #2：[B] Agent创建校验增强与Bug修复

**Assignee**：成员B

**描述**：

修改 `js/app.js`：
1. `saveConfig()` 增加字段校验：名称不能为空、名称不能是纯空格、名称不超过20字符。校验不通过时用 Toast 给出错误提示并中断保存。
2. 修复至少1个边界Bug（候选：删除最后一个agent后空状态显示、快速双击保存导致重复创建、编辑时切换性别预设被意外清空）。

**验收标准**：
- [ ] 空名称点保存 → 红色错误提示弹出，不执行保存
- [ ] 修复的Bug不再复现，无新错误
- [ ] 创建→编辑→删除流程正常

---

# Issue #3：[C] 预设人格模板扩充与润色引擎优化

**Assignee**：成员C

**描述**：

修改 `js/app.js`：
1. 为至少2种预设人格补充 `samples`（对话示例），每种2组，格式与现有条目一致。
2. 为 `_analyzePrompt()` 润色引擎增加1个评分维度（如"创造性"），含评分逻辑和优化建议文案。
3. 优化至少2种预设的 `prompt` 文案，使其更口语化自然。

**验收标准**：
- [ ] 新增samples可在预设详情面板中正常展示
- [ ] 润色面板展示维度从5个变为6个，新维度评分合理
- [ ] 预设选择和预览功能不受影响

---

# Issue #4：[D] 对话数据库扩展与情感匹配优化

**Assignee**：成员D

**描述**：

修改 `echo-care.html`：
1. 新增2个对话话题（如"学习/考试"、"宠物"），每个含3组关键词和按强度分档的回复（至少6条）。
2. 将页面中硬编码的角色名称和标题提取为顶部可配置对象。
3. 修复至少1个情感匹配Bug（如关键词交叉误判、长文本多情绪混淆）。

**验收标准**：
- [ ] 输入对应话题关键词 → 返回正确话题的回复
- [ ] 修改配置对象即可变更角色名称和欢迎语
- [ ] 修复的匹配Bug不再复现

---

# Issue #5：[E] 分享码健壮性与全流程测试

**Assignee**：成员E

**描述**：

修改 `js/app.js`：
1. `importShareCode()` 增加分类错误提示（空码/格式无效/解码失败/重复导入）。
2. 分享码去重：同一配置不重复生成多条分享记录。

编写并执行全流程测试清单（11条），覆盖：
Agent创建→配置→保存→分享→导入→情感对话→备份恢复。

**验收标准**：
- [ ] 输入各类异常分享码时显示对应提示
- [ ] 同配置重复生成分享，列表仅保留1条
- [ ] 11条测试用例全部通过
- [ ] 所有PR合并后完成集成验证
